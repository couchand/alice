// An example of Alice violations

// file-class name mismatch, class naming conventions

public class notViolations
{
    // inconsistent whitespace
    	
    // trailing whitespace
        
    // constants should be static
    public final PI = 3;

    // constant naming conventions
    public static final notPi = 4;

    // line-length limit
    public static transient Integer myVeryLongNameForAVerySimpleConcept = 42;

    // block-length limit
    private static void doSomething()
    {
        while ( checkFor() && farToo() && many() && conditions() && inThe() && one() && block() &&
                thisMakes() && thisCode() && entirely() && unreadable() && andUnmaintainable()  )
        {

        // proceeding with 300 lines of whitespace.












































































































































































































































































































        }
    }
}
